# Build context for production image — keep lean and free of secrets/runtime junk

.git
.gitignore
.venv
venv
ENV
env

# Local runtime / secrets
.env
.env.*
!.env.example
var/
staticfiles/
*.sqlite3
db.sqlite3

# Dev / IDE / OS
.idea
.vscode
*.swp
*~
.DS_Store
Thumbs.db
__pycache__
*.py[cod]
*.egg-info
.mypy_cache
.pytest_cache
.coverage
htmlcov

# Agent / local caches
.antigravitycli
.codex_tmp
.gemini
brain
.grok

# Docs not required at runtime (keep README if useful; optional)
# docs/

# data/ and data/media/ are included so RUN_BOOTSTRAP=full can load the snapshot.
